"use strict"; // treated as newer version of JS

//alert(3+3) // we are using node.js not browser that'swhy it's throwing err

console.log(3
    +3);  // code readability should be high not like this

    // tc39 and mdn documentation for any query in js

let name="Sunny Siddhu"
let age=22
let isLoggedIn=false

/* 
   Datatypes
   Primitive data types
      numbers => 2 ^ 53
      string
      bigint
      boolean - true/false
      null => standalone value   ex - let name=null  - means- empty
      undefined => variable initialized but value is not assigned
      symbol => unique

    object datatypes
*/

console.log(typeof null);